spring:
  application:
    name: venta_service
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/tienda_ventas?serverTimezone=UTC
    username: root
    password: ""
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: https://eureka-server:8761/eureka