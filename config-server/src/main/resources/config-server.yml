server:
  port: 8085
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/naimcorujo2003/Ecommerce.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
